services:
  - type: web
    name: voice-agent-rag
    runtime: docker
    plan: free # Use "free" to avoid costs
    envVars:
      - key: HF_TOKEN
        sync: false # Set this in the Render dashboard manually
      - key: PORT
        value: 8000
    healthCheckPath: /api/health
